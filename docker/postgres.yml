version: '3'
services:
  pet-project-user-pg:
    image: postgres:16.2
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=sql_db
    ports:
      - '5432:5432'